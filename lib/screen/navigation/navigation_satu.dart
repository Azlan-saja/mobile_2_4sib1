import 'package:flutter/material.dart';

class NavigationSatu extends StatelessWidget {
  const NavigationSatu({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Layar Navigation Satu'),
      ),
    );
  }
}
